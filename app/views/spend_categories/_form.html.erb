<%= form_with(model: spend_category, local: true) do |form| %>
  <% if spend_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(spend_category.errors.count, "error") %> prohibited this spend_category from being saved:</h2>

      <ul>
      <% spend_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :spend_category_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
